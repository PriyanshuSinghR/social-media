import React from 'react';
import logo1 from '../assets/amaterasu.gif';

export const Loader = () => {
  return (
    <img
      src={logo1}
      className="App-logo"
      alt="logo"
      style={{ width: '100px', height: '100px' }}
    />
  );
};
